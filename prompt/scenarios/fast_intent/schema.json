{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FastIntentNeedsPlanArguments",
  "description": "FastIntent returns ordinary assistant text for direct replies. For all executable work it calls needs_plan with these arguments.",
  "type": "object",
  "additionalProperties": false,
  "required": ["confidence", "reason"],
  "properties": {
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "reason": {
      "type": "string"
    }
  }
}
